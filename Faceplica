# Faceplica — aedificatio libison.a
#
# Usus:
#   face            — aedifica libison.a
#   face purga      — dele omnia aedificata
#   face proba      — aedifica et curre probationes (si adsunt)

CC      ?= cc
AR      ?= ar
CFLAGS  ?= -Wall -Wextra -pedantic -std=c99 -O2
ARFLAGS  = rcs

FONTES  = ison.c
OBJECTA = $(FONTES:.c=.o)
BIBLIOTHECA = libison.a

omnia: $(BIBLIOTHECA)

$(BIBLIOTHECA): $(OBJECTA)
	$(AR) $(ARFLAGS) $@ $^

%.o: %.c ison.h
	$(CC) $(CFLAGS) -c -o $@ $<

PREFIX = /opt/apotheca

colloca: omnia
	install -d $(PREFIX)/lib $(PREFIX)/include/ison
	install -m 644 $(BIBLIOTHECA) $(PREFIX)/lib/
	install -m 644 ison.h $(PREFIX)/include/ison/

purga:
	rm -f $(OBJECTA) $(BIBLIOTHECA) proba_ison

linte:
	insinulint -c ison.c ison.h proba_ison.c

proba_ison: proba_ison.c $(BIBLIOTHECA)
	$(CC) $(CFLAGS) -o $@ $< -L. -lison

proba: proba_ison
	./proba_ison

.PHONY: omnia colloca purga proba linte
